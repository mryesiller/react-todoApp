sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mryesiller/react-todoapp -f Dockerfile.dev .

deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_ACCESS_KEY
